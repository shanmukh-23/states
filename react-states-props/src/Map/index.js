function Map(props) {
    const unreadMessages = props.unreadMessages;
    const a = unreadMessages.Map((nam)=> {})
    return (
      <div>
        <h1>Hello!</h1>
        {unreadMessages.nam}
      </div>
    );
  }
  

export  default Map


